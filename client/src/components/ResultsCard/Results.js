import React from "react";

const Result = props => (
    <div class="card">
        <div class="card-body">
            
        </div>

        <button type="submit" class="btn btn-primary">Save</button>
    </div>

);

export default Result;